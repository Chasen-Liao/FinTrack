# 项目七：基于 AI 算法的量化交易策略课程设计报告

> 报告命名占位：项目七_专业_学号_姓名  
> 专业：__________  
> 学号：__________  
> 姓名：__________  
> 指导教师：__________  
> 完成日期：__________

## 摘要

本课程设计围绕“基于 AI 算法的量化交易策略”展开，设计并实现了一个结合股票历史行情、财经新闻情绪分析和机器学习预测的量化交易系统。系统以 Polygon.io 获取的真实股票 OHLC 行情数据和相关新闻数据为基础，经过数据清洗、新闻过滤、LLM 情感分析、交易日对齐和特征工程后，使用 XGBoost 构建股票涨跌方向预测模型，并将模型预测结果转换为买入或持仓信号进行回测。

项目选取 XGBoost 作为主要机器学习算法。该算法适合处理本项目中的结构化金融特征，例如历史收益率、波动率、成交量变化、RSI、新闻数量、新闻情绪比例等。与深度学习模型相比，XGBoost 对中小规模表格数据更加稳定，训练速度较快，并且可以输出特征重要性，便于解释模型预测依据。

在策略评估中，项目使用样本外回测思想，将模型预测概率转换为交易信号，并计算年化收益率、最大回撤、累计收益率、胜率和交易次数等指标。当前最佳策略为 MU 股票、T+5 预测周期、阈值 0.5 的策略组合。回测区间为 2024-12-16 至 2026-03-03，年化收益率为 162.99%，最大回撤为 19.00%，满足课程要求中“年化收益率超过 20%、最大回撤不超过 20%”的指标要求。

## 一、课程设计目的

本课程设计的主要目标包括以下几个方面：

1. 自主选择并实现一种适用于量化交易场景的 AI 算法。
2. 获取真实或可获取的金融市场数据，包括股票历史行情数据和相关新闻数据。
3. 完成数据清洗、缺失值处理、特征构建和训练集测试集划分。
4. 基于 AI 模型输出预测结果，并将预测结果转换为买入、卖出或持仓信号。
5. 对交易策略进行回测，计算收益率和风险指标。
6. 对策略效果进行总结，分析项目的优点、不足和后续优化方向。

通过本项目，可以加深对机器学习算法、新闻情绪分析、量化交易策略构建和回测评估方法的理解，并掌握将 AI 算法应用到实际金融数据分析中的基本流程。

## 二、课程设计环境

### 2.1 硬件与操作系统环境

本项目在本地 Windows 环境下开发和运行，主要使用 Python 后端、SQLite 数据库和 React 前端进行系统实现。

| 项目 | 说明 |
| --- | --- |
| 操作系统 | Windows 11 |
| Python 版本 | Python 3.12.4 |
| 开发工具 | VS Code / PowerShell |
| 处理器 | Intel 64 位处理器 |
| 内存 | 16GB |
| 数据库 | SQLite |
| 后端框架 | FastAPI |
| 前端框架 | React + TypeScript |
| 构建工具 | Vite |
| 可视化 | D3.js |

### 2.2 Python 环境与主要依赖

项目主要使用 Python 进行数据处理、机器学习训练和策略回测。主要依赖如下：

| 依赖库 | 用途 |
| --- | --- |
| numpy | 数值计算 |
| pandas | 表格数据处理、特征工程 |
| scikit-learn | 模型评估、机器学习辅助工具 |
| xgboost | XGBoost 分类模型训练与预测 |
| requests | 调用 Polygon.io 等外部接口 |
| fastapi | 后端 API 服务 |
| uvicorn | FastAPI 服务运行 |
| pydantic-settings | 环境变量和配置管理 |
| openai / anthropic | LLM 接口调用 |
| torch | 深度学习实验依赖 |
| pytest | 测试框架 |
| vite | 前端构建工具 |

### 2.3 数据来源

本项目的数据来源主要包括：

1. 股票行情数据：Polygon.io 提供的日频 OHLC 数据，包括开盘价、最高价、最低价、收盘价、成交量、VWAP 和交易笔数。
2. 财经新闻数据：Polygon.io 提供的股票相关新闻，包括标题、摘要、发布时间、发布来源、关联股票等。
3. 新闻情绪分析结果：通过 LLM 对新闻摘要进行相关性判断、情绪分类和原因提取。

当前本地数据库中已经保存了较大规模的实验数据，包括 105 只股票、56676 条 OHLC 行情数据、63831 条原始新闻数据、117078 条新闻交易日对齐数据和 110970 条 Layer 1 新闻情绪分析结果。

## 三、课程设计原理

### 3.1 量化交易策略整体思路

本项目的基本思路是：股票价格变化不仅受到历史价格走势、成交量和技术指标影响，也可能受到相关新闻事件影响。因此，项目将行情数据和新闻情绪数据结合起来，构建机器学习特征，并训练模型预测未来一段时间内股票价格是否上涨。

整体流程如下：

1. 获取股票历史行情和相关新闻。
2. 对原始新闻进行规则过滤，删除空摘要、过短摘要、市场综述和榜单类文章。
3. 使用 LLM 对新闻进行批量情绪分析，提取新闻相关性、情感方向、摘要、上涨原因和下跌原因。
4. 将新闻发布时间对齐到交易日，并计算新闻发布后 T+0、T+1、T+3、T+5、T+10 收益。
5. 按交易日聚合新闻特征，并结合价格技术指标构建机器学习特征。
6. 使用 XGBoost 模型预测未来 T+1 或 T+5 股票价格方向。
7. 根据模型输出概率和阈值生成交易信号。
8. 进行策略回测，计算收益和风险指标。

### 3.2 XGBoost 算法原理

XGBoost 是一种基于梯度提升树的机器学习算法。它通过逐步训练多棵决策树，使后一棵树不断修正前面模型的预测误差，最终形成一个强预测模型。

在二分类任务中，XGBoost 的输入是特征矩阵，输出是样本属于正类的概率。本项目中，正类表示未来指定周期后股票收盘价高于当前收盘价，即股票价格上涨。

XGBoost 适用于本项目的原因包括：

1. 本项目特征以结构化表格数据为主，XGBoost 对表格数据表现稳定。
2. 金融数据中的特征关系通常是非线性的，XGBoost 可以捕捉复杂的非线性关系。
3. XGBoost 对不同尺度的特征不敏感，适合同时处理收益率、成交量比例、情绪分数等不同类型特征。
4. 相比 LSTM、Transformer 等深度学习模型，XGBoost 对样本量要求较低，更适合课程项目中的中小规模数据。
5. XGBoost 可以输出特征重要性，便于解释模型预测依据。

### 3.3 可选算法对比

除 XGBoost 外，项目也可以使用其他机器学习算法。几种可选算法对比如下：

| 算法 | 优点 | 缺点 | 适用性 |
| --- | --- | --- | --- |
| Logistic Regression | 简单、可解释性强 | 难以表达复杂非线性关系 | 适合作为基线模型 |
| Random Forest | 稳定、抗过拟合能力较强 | 对趋势细节的刻画通常不如提升树 | 适合作为对照算法 |
| LightGBM | 训练速度快，适合大规模特征 | 小样本下需要注意过拟合 | 可作为 XGBoost 的替代或增强 |
| SVM | 适合中小规模非线性分类 | 对参数和特征缩放敏感 | 可用于实验对比 |
| LSTM | 适合序列建模 | 数据需求较高，训练和解释更复杂 | 适合作为拓展实验 |

综合考虑数据规模、特征类型、训练稳定性和报告解释性，本项目选择 XGBoost 作为主模型。

### 3.4 特征工程原理

项目构建的特征主要分为新闻特征和价格技术特征两类。

新闻特征包括：

1. 当日相关新闻数量。
2. 高相关或中相关新闻数量。
3. 正面、负面、中性新闻数量。
4. 情绪分数，即正面新闻数量减去负面新闻数量后除以新闻总数。
5. 正面新闻比例和负面新闻比例。
6. 3 日、5 日、10 日滚动新闻情绪均值。
7. 新闻数量滚动窗口统计。
8. 情绪动量，即短期情绪均值与长期情绪均值的差值。

价格技术特征包括：

1. 1 日、3 日、5 日、10 日历史收益率。
2. 5 日和 10 日波动率。
3. 5 日成交量比例。
4. 跳空幅度。
5. MA5 与 MA20 的相对位置。
6. RSI 14 指标。
7. 星期几特征。

为了减少未来函数问题，价格类技术指标大多使用 `shift(1)` 处理，即只使用当前交易日前已经能够观察到的信息。

### 3.5 交易信号生成逻辑

模型输出未来上涨的概率 `prob_up`。当 `prob_up` 大于等于设定阈值时，策略生成**买入信号**；当 `prob_up` 低于阈值时，策略生成**卖出信号**（若当前有持仓则平仓，空仓则保持空仓）。

具体开平仓规则如下：

1. **买入开仓**：当 `prob_up >= 阈值` 且当前无持仓时，以当日收盘价买入股票，并扣除买入手续费（费率 0.1%）。
2. **持仓不动**：当 `prob_up >= 阈值` 且当前已持仓时，继续持有股票，不做任何操作。
3. **卖出平仓**：当 `prob_up < 阈值` 且当前有持仓时，以当日收盘价卖出股票，并扣除卖出手续费（费率 0.1%）。
4. **空仓观望**：当 `prob_up < 阈值` 且当前无持仓时，保持空仓，资金不产生收益。

当前最佳策略使用：

| 项目 | 设置 |
| --- | --- |
| 股票 | MU |
| 预测周期 | T+5 |
| 买入阈值 | 0.5 |
| 手续费率 | 0.001 |
| 交易方式 | 多头 / 空仓（不做卖空） |

该策略不做卖空，只在模型认为未来上涨概率较高时持有股票，否则保持空仓。策略持仓周期不固定，完全由模型每日预测信号驱动。

### 3.6 回测指标计算方法

本项目使用以下指标评价策略效果：

1. 累计收益率：回测结束时策略净值相对初始净值的增长比例。
2. 年化收益率：根据交易日数量将累计收益折算为年化收益。
3. 最大回撤：策略净值从历史高点到后续低点的最大跌幅。
4. 胜率：盈利交易次数占总交易次数的比例。
5. 交易次数：回测期间完整买入卖出交易的次数。
6. 买入持有收益率：作为基准，用于比较策略与简单持有股票的表现。

最大回撤的计算方式为：

```text
最大回撤 = max((历史最高净值 - 当前净值) / 历史最高净值)
```

年化收益率的计算方式为：

```text
年化收益率 = (期末净值 / 期初净值) ^ (252 / 交易日数量) - 1
```

## 四、课程设计步骤和结果

### 4.1 数据获取

项目通过 `backend/polygon/client.py` 中的接口函数获取股票行情和新闻数据。行情数据使用 Polygon.io 的日频聚合接口，新闻数据使用 Polygon.io 的新闻接口。

主要数据获取函数包括：

```python
fetch_ohlc(ticker: str, start: str, end: str)
fetch_news(ticker: str, start: str, end: str)
search_tickers(query: str, limit: int)
```

获取到的数据保存到本地 SQLite 数据库 `pokieticker.db` 中。数据库主要表包括：

| 数据表 | 作用 |
| --- | --- |
| tickers | 股票基本信息 |
| ohlc | 日频行情数据 |
| news_raw | 原始新闻 |
| news_ticker | 新闻和股票关联关系 |
| layer0_results | 规则过滤结果 |
| layer1_results | LLM 情绪分析结果 |
| news_aligned | 新闻与交易日对齐结果 |

### 4.2 数据预处理

数据预处理主要包括新闻过滤、情绪分析和交易日对齐。

首先，项目使用 Layer 0 规则过滤明显无关或质量较低的新闻，过滤规则包括：

1. 空摘要过滤。
2. 摘要长度过短过滤。
3. 涉及股票过多且目标股票不在标题中的市场综述过滤。
4. Top、Best、Worst 等榜单类文章过滤。

其次，项目使用 Layer 1 对通过规则过滤的新闻进行批量 LLM 分析，输出新闻相关性、情绪方向、中文摘要、上涨原因和下跌原因。

最后，项目将新闻发布时间对齐到最近交易日，并计算新闻发布后的 T+0、T+1、T+3、T+5、T+10 收益，便于后续分析新闻事件和股票收益之间的关系。

### 4.3 特征构建

项目以交易日为粒度构建特征矩阵。每一行表示某只股票在某个交易日的特征和未来收益方向标签。

特征构建过程包括：

1. 从 `ohlc` 表读取股票日频行情。
2. 从 `news_aligned` 和 `layer1_results` 聚合每日新闻情绪数据。
3. 将新闻特征合并到交易日行情数据。
4. 构建历史收益率、波动率、成交量比例、均线、RSI 等技术指标。
5. 构建未来 T+1、T+2、T+3、T+5 涨跌方向标签。

模型使用的目标变量为：

```text
target_t5 = 未来第 5 个交易日收盘价是否高于当前收盘价
```

如果未来第 5 个交易日收盘价高于当前收盘价，则标签为 1，否则为 0。

### 4.4 模型训练

项目使用 XGBoost 分类器训练股票涨跌方向预测模型。训练时按照时间顺序划分训练集和测试集，避免随机划分导致的时间序列信息泄露。

模型主要参数如下：

| 参数 | 值 |
| --- | --- |
| max_depth | 4 |
| n_estimators | 200 或 300 |
| learning_rate | 0.05 |
| subsample | 0.8 |
| colsample_bytree | 0.8 |
| eval_metric | logloss |
| random_state | 42 |

训练完成后，模型保存为 `.joblib` 文件，模型元信息保存为 JSON 文件。MU 股票 T+5 模型在测试集上的评估指标如下：

| 指标 | 数值 |
| --- | --- |
| 训练集样本数 | 401 |
| 测试集样本数 | 101 |
| 训练区间 | 2024-03-01 至 2025-10-06 |
| 测试区间 | 2025-10-07 至 2026-03-03 |
| 准确率 | 61.39% |
| 基线准确率 | 69.31% |
| 精确率 | 72.46% |
| 召回率 | 71.43% |
| F1 值 | 71.94% |

特征重要性排名前 10 的特征如下：

| 排名 | 特征名称 | 重要性 |
| --- | --- | --- |
| 1 | 3日正面新闻比例 (positive_ratio_3d) | 0.0554 |
| 2 | 3日情绪分数 (sentiment_score_3d) | 0.0507 |
| 3 | 10日新闻数量 (news_count_10d) | 0.0472 |
| 4 | RSI 14 (rsi_14) | 0.0466 |
| 5 | 10日波动率 (volatility_10d) | 0.0439 |
| 6 | 10日正面新闻比例 (positive_ratio_10d) | 0.0418 |
| 7 | 5日正面新闻比例 (positive_ratio_5d) | 0.0408 |
| 8 | MA5 vs MA20 (ma5_vs_ma20) | 0.0396 |
| 9 | 10日情绪分数 (sentiment_score_10d) | 0.0370 |
| 10 | 3日负面新闻比例 (negative_ratio_3d) | 0.0369 |

从特征重要性可以看出，新闻情绪特征（尤其是滚动窗口内的正面新闻比例和情绪分数）和价格技术指标（RSI、波动率、均线位置）共同对模型预测贡献最大，说明将新闻情绪与价格特征结合的思路是有效的。

### 4.5 策略回测

策略回测由 `backend/ml/strategy_backtest.py` 实现。回测过程如下：

1. 构建指定股票的机器学习特征。
2. 使用扩展窗口方式生成样本外预测结果。
3. 根据预测上涨概率和阈值生成交易信号。
4. 按日模拟策略净值变化。
5. 在持仓变化时扣除手续费。
6. 统计交易记录、收益曲线和关键指标。

扩展窗口回测的思想是：前期历史数据作为训练集，之后一段数据作为测试集；随着时间推进，训练窗口不断扩展。这种方法更符合金融时间序列预测场景，可以减少使用未来数据的风险。

### 4.6 策略运行结果

当前最佳策略结果如下：

| 指标 | 数值 |
| --- | --- |
| 股票 | MU |
| 预测周期 | T+5 |
| 阈值 | 0.5 |
| 回测开始日期 | 2024-12-16 |
| 回测结束日期 | 2026-03-03 |
| 年化收益率 | 162.99% |
| 最大回撤 | 19.00% |
| 累计收益率 | 217.39% |
| 买入持有收益率 | 250.71% |
| 胜率 | 66.67% |
| 交易次数 | 39 |
| 平均单笔收益率 | 3.35% |
| 最佳单笔收益率 | 23.15% |
| 最差单笔收益率 | -10.22% |
| 是否满足课程指标 | 是 |

根据课程要求，策略应尽量达到：

| 指标 | 要求 | 当前结果 | 是否满足 |
| --- | --- | --- | --- |
| 年化收益率 | 超过 20% | 162.99% | 满足 |
| 最大回撤 | 不超过 20% | 19.00% | 满足 |

因此，从当前回测结果看，项目达到了课程设计中关于收益率和最大回撤的核心要求。

### 4.7 策略收益曲线

下图展示了 MU 股票 T+5 策略（阈值 0.5）在回测区间内的净值变化曲线，并与同期买入持有（Buy & Hold）策略进行了对比：

![策略收益曲线](../image/strategy_equity_curve.png)

从图中可以看出：

1. **策略净值曲线（蓝色实线）**：策略整体呈上升趋势，最终净值达到约 3.17（累计收益率 217.39%）。策略在 2025 年 4 月至 5 月期间有一次明显上涨，净值从约 1.31 快速拉升至约 1.61，主要受益于该阶段 MU 股票的强势表现。最高净值出现在 2026-02-20，达到 3.21。

2. **买入持有净值曲线（红色虚线）**：买入持有策略的最终净值约为 3.51（累计收益率 250.71%），略高于策略净值。这说明在回测区间内，MU 股票本身处于较强的上升趋势中。

3. **策略与基准对比**：策略净值大部分时间低于买入持有净值，但在市场回调阶段（如 2025 年 6 月至 7 月、2025 年 11 月至 12 月），策略通过空仓或轻仓避免了部分下跌，从而降低了最大回撤。策略最大回撤为 19.00%（发生在 2025-11-03 至 2025-11-20），而买入持有策略同期回撤更大。

4. **交易明细分析**：回测期间共完成 39 笔交易，其中盈利 26 笔（胜率 66.67%），亏损 13 笔。平均盈利 6.67%，平均亏损 -3.28%，盈亏比约为 2.03，说明策略在控制亏损的同时能够获取较好的单笔收益。最佳单笔交易为 2025-04-04 至 2025-04-25，收益率达 23.15%；最差单笔交易为 2025-06-26 至 2025-07-21，亏损 -10.22%。

5. **持仓与空仓分布**：回测区间共 302 个交易日，策略持仓 165 天（占比 54.6%），空仓 137 天（占比 45.4%）。空仓期主要集中在市场方向不明或下跌趋势阶段，如 2025 年 5 月下旬至 6 月初、2026 年 2 月下旬等，体现了策略的风险规避能力。最低净值 0.93 出现在 2024-12-31，为策略刚启动初期的正常波动。

### 4.8 结果截图说明

项目已经保存部分系统运行和分析截图，可放入最终报告中：

| 图片 | 建议说明 |
| --- | --- |
| `image-1.png` | 系统整体图或策略结果展示 |
| `image.png` | MU 股票策略或预测展示 |
| `image-2.png` | AI 深度分析截图 |
| `image-3.png` | 区间分析截图 |
| `image-4.png` | 系统总览界面 |
| `image/新闻.png` | 新闻分析界面 |
| `image/区间分析.png` | 区间分析界面 |
| `image/strategy_equity_curve.png` | 策略净值曲线 vs 买入持有 |

最终提交 Word 或 PDF 报告时，可以将上述图片插入到“课程设计步骤和结果”章节中。

## 五、课程设计代码说明

项目代码主要分为后端、前端、机器学习和数据处理几个部分。

### 5.1 项目目录结构

```text
PokieTicker/
├── backend/
│   ├── api/                 # FastAPI 接口
│   ├── llm/                 # LLM 客户端
│   ├── ml/                  # 机器学习训练、预测和回测
│   ├── pipeline/            # 新闻过滤、情绪分析、交易日对齐
│   ├── polygon/             # Polygon.io 数据获取
│   ├── config.py            # 环境配置
│   └── database.py          # SQLite 数据库结构
├── frontend/                # React 前端
├── tests/                   # 测试代码
├── image/                   # 报告和 README 图片
├── pokieticker.db           # 本地 SQLite 数据库
├── README.md                # 项目说明
└── requirements.txt         # Python 依赖
```

### 5.2 核心代码文件

| 文件 | 作用 |
| --- | --- |
| `backend/polygon/client.py` | 获取股票行情和新闻数据 |
| `backend/database.py` | 定义 SQLite 数据库结构 |
| `backend/pipeline/layer0.py` | 新闻规则过滤 |
| `backend/pipeline/layer1.py` | LLM 批量新闻情绪分析 |
| `backend/pipeline/alignment.py` | 新闻与交易日对齐并计算未来收益 |
| `backend/ml/features.py` | 构建机器学习特征 |
| `backend/ml/model.py` | XGBoost 模型训练与预测 |
| `backend/ml/backtest.py` | 扩展窗口交叉验证 |
| `backend/ml/strategy_backtest.py` | 交易策略回测与指标计算 |
| `backend/api/routers/predict.py` | 策略结果和预测结果 API |

### 5.3 项目运行说明

安装依赖：

```powershell
cd E:\MyProjects\Agent_Projects\pokieticker\PokieTicker
.\venv\Scripts\activate
pip install -r requirements.txt
```

启动后端：

```powershell
cd E:\MyProjects\Agent_Projects\pokieticker\PokieTicker
.\venv\Scripts\python.exe -m uvicorn backend.api.main:app --host 127.0.0.1 --port 8000
```

启动前端：

```powershell
cd E:\MyProjects\Agent_Projects\pokieticker\PokieTicker\frontend
npm run dev
```

运行策略扫描：

```powershell
cd E:\MyProjects\Agent_Projects\pokieticker\PokieTicker
.\venv\Scripts\python.exe -m backend.ml.strategy_backtest --scan
```

运行单只股票策略回测：

```powershell
.\venv\Scripts\python.exe -m backend.ml.strategy_backtest --symbol MU --horizon t5 --threshold 0.5
```

策略结果文件：

```text
backend/ml/models/strategy_best.json
backend/ml/models/MU_t5_thr50_strategy_backtest.json
```

代码链接占位：

```text
请在此处填写 GitHub / Gitee / 课程平台代码链接：____________________
```

## 六、课程设计总结

### 6.1 项目完成情况

本项目完成了基于 AI 算法的量化交易策略设计与实现，主要完成内容包括：

1. 使用 Polygon.io 获取股票历史行情和相关新闻数据。
2. 建立 SQLite 数据库保存股票、行情、新闻、情绪分析和回测结果。
3. 实现新闻规则过滤、LLM 情绪分析和新闻交易日对齐。
4. 构建新闻情绪特征和价格技术指标。
5. 使用 XGBoost 训练股票涨跌方向预测模型。
6. 将模型预测结果转换为交易信号。
7. 实现策略回测，计算年化收益率、最大回撤、累计收益率、胜率和交易次数。
8. 构建前端页面展示新闻分析、区间分析、预测结果和策略回测结果。

从当前最佳回测结果看，策略年化收益率为 162.99%，最大回撤为 19.00%，满足课程设计要求中的关键指标。

### 6.2 项目优点

本项目的主要优点包括：

1. 数据来源真实，使用实际股票行情和新闻数据，而不是人工构造数据。
2. 策略流程完整，覆盖数据获取、预处理、特征工程、模型训练、信号生成和回测分析。
3. 特征设计较丰富，同时考虑价格技术指标和新闻情绪指标。
4. 使用 XGBoost 作为主模型，适合结构化金融数据，并具有较好的可解释性。
5. 回测指标较完整，包含课程要求的年化收益率、最大回撤、累计收益率、胜率和交易次数。
6. 系统具有前端展示界面，能够直观查看新闻分析和策略表现。

### 6.3 遇到的问题与处理方法

在项目实现过程中，主要遇到以下问题：

1. 新闻数据质量不稳定。部分新闻摘要为空、过短或属于市场综述文章，直接用于模型会引入噪声。项目通过 Layer 0 规则过滤降低低质量新闻影响。
2. 新闻与交易日不完全匹配。新闻可能发布在非交易日或盘后时间，因此项目将新闻发布时间对齐到最近交易日，并计算多个未来周期收益。
3. LLM 情绪分析成本较高。项目采用批量处理方式，将多篇新闻合并分析，以降低调用成本。
4. 金融时间序列容易出现未来函数。项目在价格技术指标中使用历史窗口和 `shift(1)`，并在回测中使用扩展窗口样本外预测。
5. 单一股票策略可能存在偶然性。当前最佳策略集中在 MU 股票，后续可以增加多股票组合回测来提高稳健性。

### 6.4 不足之处

项目仍存在一些不足：

1. 当前主模型为 XGBoost，虽然适合表格特征，但未充分比较 Random Forest、LightGBM、Logistic Regression 等其他算法。
2. 新闻发布时间的盘中、盘后影响仍可进一步细化，当前对新闻时序的处理还可以更严格。
3. 回测中虽然考虑了手续费，但尚未充分考虑滑点、流动性和实际成交限制。
4. 当前最佳策略虽然满足课程指标，但买入持有收益率（250.71%）高于策略收益率（217.39%），说明部分收益来自 MU 自身强趋势。策略虽然通过择时避免了部分回撤，但在 MU 持续上涨的行情中，频繁的开平仓和空仓期导致策略未能完全捕捉到全部涨幅。此外，手续费和信号滞后也在一定程度上侵蚀了收益。这反映出当前策略在强趋势市场中的局限性：模型基于 T+5 预测周期生成信号，对市场短期趋势的响应速度有限。
5. 多股票组合层面的资金管理和风险控制仍较简单。
6. 对模型预测错误案例的分析还可以更加深入。

### 6.5 后续优化方向

后续可以从以下方面继续改进：

1. 增加 Random Forest 或 LightGBM 作为对比模型，验证 XGBoost 算法选择的合理性。
2. 建立算法对比实验表，对比买入持有、价格技术指标模型、价格加新闻情绪模型和不同机器学习算法的效果。
3. 引入市场基准特征，例如 SPY、QQQ、行业 ETF 收益率和市场波动率，以区分个股新闻影响和整体市场影响。
4. 优化新闻特征，将新闻按财报、评级、产品、诉讼、并购等事件类型分类，并赋予不同权重。
5. 增加概率校准和动态阈值，根据市场波动率和股票特性调整买入门槛。
6. 在回测中加入滑点和更严格的交易时序，提升策略评估的真实性。
7. 增加可解释性分析，展示关键交易日的新闻、模型概率、特征贡献和实际收益。

## 七、结论

本课程设计实现了一个基于 AI 算法的量化交易策略系统。项目以真实股票行情和财经新闻为数据基础，通过新闻情绪分析和技术指标构建机器学习特征，使用 XGBoost 模型预测股票未来涨跌方向，并将预测结果转换为交易信号进行回测。

实验结果表明，当前最佳策略在 MU 股票上取得了较好的回测表现。该策略在 2024-12-16 至 2026-03-03 的回测区间内，年化收益率达到 162.99%，最大回撤为 19.00%，满足课程要求中“年化收益率超过 20%、最大回撤不超过 20%”的指标要求。

总体来看，本项目完成了 AI 算法选择、金融数据获取、特征工程、模型训练、交易信号生成、策略回测和结果分析等完整流程，达到了课程设计目标。后续可以进一步加入多算法对比、市场基准特征、严格交易时序和多股票组合回测，以提升策略的稳定性和实际应用价值。

## 附录 A：关键结果截图

> 最终排版时请将以下图片插入到对应位置。

![系统总览](image-4.png)

![系统图](image-1.png)

![MU 策略展示](image.png)

![AI 深度分析](image-2.png)

![区间分析](image-3.png)

## 附录 B：关键命令

```powershell
# 启动后端
.\venv\Scripts\python.exe -m uvicorn backend.api.main:app --host 127.0.0.1 --port 8000

# 启动前端
cd frontend
npm run dev

# 扫描所有策略
.\venv\Scripts\python.exe -m backend.ml.strategy_backtest --scan

# 运行最佳策略
.\venv\Scripts\python.exe -m backend.ml.strategy_backtest --symbol MU --horizon t5 --threshold 0.5
```
